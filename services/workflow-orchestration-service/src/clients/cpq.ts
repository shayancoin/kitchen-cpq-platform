// Minimal CPQ client placeholder. Replace with real HTTP/gRPC implementation.
export const cpqClient = {
  async recomputeQuote(input: { projectId: string }): Promise<void> {
    // TODO: integrate with CPQ service endpoint.
    return;
  }
};
