{
  "name": "shared-events",
  "root": "libs/shared-events",
  "sourceRoot": "libs/shared-events/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"build shared-events (stub)\""
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"lint shared-events (stub)\""
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"test shared-events (stub)\""
      }
    }
  },
  "tags": ["type:lib", "scope:shared"]
}
