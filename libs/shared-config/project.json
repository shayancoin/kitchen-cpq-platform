{
  "name": "shared-config",
  "root": "libs/shared-config",
  "sourceRoot": "libs/shared-config/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"build shared-config (stub)\""
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"lint shared-config (stub)\""
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"test shared-config (stub)\""
      }
    }
  },
  "tags": ["type:lib", "scope:shared"]
}

