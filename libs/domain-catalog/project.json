{
  "name": "domain-catalog",
  "root": "libs/domain-catalog",
  "sourceRoot": "libs/domain-catalog/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"build domain-catalog (stub)\""
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"lint domain-catalog (stub)\""
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run libs/domain-catalog/src --config libs/domain-catalog/vitest.config.ts"
      }
    }
  },
  "tags": ["type:lib", "scope:domain"]
}
