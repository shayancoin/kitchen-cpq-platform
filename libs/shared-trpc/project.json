{
  "name": "shared-trpc",
  "root": "libs/shared-trpc",
  "sourceRoot": "libs/shared-trpc/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"build shared-trpc (stub)\""
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"lint shared-trpc (stub)\""
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"test shared-trpc (stub)\""
      }
    }
  },
  "tags": ["type:lib", "scope:shared"]
}
