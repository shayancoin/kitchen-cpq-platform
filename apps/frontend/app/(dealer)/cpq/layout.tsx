import type { ReactNode } from 'react';
import BaseLayout from '../../cpq/layout';

export default function DealerCpqLayout({ children }: { children: ReactNode }) {
  return <BaseLayout>{children}</BaseLayout>;
}
